<?php
/**
 * Implementation of hook_init().
 */
function region_filter_init() {

}
/*
function region_filter_nodeapi(&$node, $op, $a3 = NULL, $a4 = NULL) {
  switch ($op) {

    case 'view':
       $region = getRegion();
        foreach($node->taxonomy as $term) {

          if($term->name != $region ) {
            $link = 'http://' . $term->name . '.thedelimagazine.com/' . drupal_get_path_alias("node/$node->nid", $path_language = '');
 
        }
      break;
  }
}
*/